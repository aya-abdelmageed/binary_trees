#include "binary_trees.h"

/**
 * binary_tree_is_root - fun that checks if a given node is a root
 * @node: a pointer to the node to check
 * Return: 1 if yes 0 if no
 */

int binary_tree_is_root(const binary_tree_t *node)
{
	if (node == NULL)
		return (0);

	if (parent == NULL)
		return (1);
	else
		return (0);
}
